{
  "name": "aravadistillery-backend",
  "version": "1.0.0",
  "description": "Shared Vercel serverless backend for Arava Distillery apps (Factory Control + CRM)",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
